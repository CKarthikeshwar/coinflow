/**
 * Web has no SMS receiver and no background tasks (D3). This stub keeps `import
 * '@/services/tasks'` in the app entry harmless on the static web build.
 */

export const SMS_INGEST_TASK = 'CoinflowSmsIngest';
export const NOTIFICATION_RESPONSE_TASK = 'coinflow.NOTIFICATION_RESPONSE';
