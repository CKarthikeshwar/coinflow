// Placeholder so `jest` has something green to run after the scaffolding pass.
// Delete once the first real `src/domain` unit tests land (SPEC-implementation.md §34.1).

describe('scaffold', () => {
  it('runs the test toolchain', () => {
    expect(1 + 1).toBe(2);
  });
});
