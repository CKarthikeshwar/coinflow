diff --git a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheet/BottomSheet.tsx b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheet/BottomSheet.tsx
index fc52033..ad5e92a 100644
--- a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheet/BottomSheet.tsx
+++ b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheet/BottomSheet.tsx
@@ -1084,6 +1084,47 @@ const BottomSheetComponent = forwardRef<BottomSheet, BottomSheetProps>(
         isLayoutCalculated,
       ]
     );
+
+    /**
+     * Ensure the mount position evaluation runs even when the animated
+     * reaction that normally triggers it never fires.
+     *
+     * On Reanimated v4, every `useAnimatedReaction` attached to a component
+     * instance can fail to fire when the instance mounts while the JS thread
+     * is busy: derived values (detents, isLayoutCalculated) still compute
+     * correctly, but the reaction that evaluates the initial position never
+     * runs, leaving the sheet parked off-screen at the container height.
+     *
+     * This effect re-dispatches the same evaluation from the JS side until
+     * the mount has been handled, then stops. When the reactions are healthy
+     * the first check observes `didAnimateOnMount` already true and does
+     * nothing. Patch from https://github.com/gorhom/react-native-bottom-sheet/pull/2720
+     * (unmerged upstream) applied locally via patch-package.
+     */
+    useEffect(() => {
+      let attempts = 0;
+      const enforceMountPosition = setInterval(() => {
+        if (didAnimateOnMount.value || attempts >= 20) {
+          clearInterval(enforceMountPosition);
+          return;
+        }
+        attempts += 1;
+        runOnUI(() => {
+          'worklet';
+          if (didAnimateOnMount.value) {
+            return;
+          }
+          if (
+            animatedAnimationState.get().status === ANIMATION_STATUS.RUNNING
+          ) {
+            return;
+          }
+          evaluatePosition(ANIMATION_SOURCE.MOUNT);
+        })();
+      }, 100);
+      return () => clearInterval(enforceMountPosition);
+      // eslint-disable-next-line react-hooks/exhaustive-deps
+    }, []);
     //#endregion
 
     //#region public methods
@@ -1811,6 +1852,7 @@ const BottomSheetComponent = forwardRef<BottomSheet, BottomSheetProps>(
               containerLayoutState={containerLayoutState}
               topInset={topInset}
               bottomInset={bottomInset}
+              modal={$modal}
               detached={detached}
               style={_providedContainerStyle}
             >
diff --git a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx
index 879061d..102fa1f 100644
--- a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx
+++ b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx
@@ -17,6 +17,7 @@ function BottomSheetHostingContainerComponent({
   layoutState,
   topInset = 0,
   bottomInset = 0,
+  modal,
   shouldCalculateHeight = true,
   detached,
   style,
@@ -61,6 +62,9 @@ function BottomSheetHostingContainerComponent({
         layoutState.modify(state => {
           'worklet';
           state.rawContainerHeight = height;
+          state.containerHeight = modal
+            ? height - topInset - bottomInset
+            : height;
           return state;
         });
       }
@@ -108,7 +112,7 @@ function BottomSheetHostingContainerComponent({
         });
       }
     },
-    [layoutState, containerLayoutState]
+    [layoutState, containerLayoutState, modal, topInset, bottomInset]
   );
   //#endregion
 
diff --git a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/types.d.ts b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/types.d.ts
index ed43773..80d8466 100644
--- a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/types.d.ts
+++ b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetHostingContainer/types.d.ts
@@ -10,6 +10,7 @@ export interface BottomSheetHostingContainerProps
   > {
   containerLayoutState?: SharedValue<ContainerLayoutState>;
   layoutState?: SharedValue<LayoutState>;
+  modal?: boolean;
 
   shouldCalculateHeight?: boolean;
   style?: StyleProp<ViewStyle>;
diff --git a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetModal/BottomSheetModal.tsx b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetModal/BottomSheetModal.tsx
index 2954b3f..6efeb9b 100644
--- a/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetModal/BottomSheetModal.tsx
+++ b/node_modules/@gorhom/bottom-sheet/src/components/bottomSheetModal/BottomSheetModal.tsx
@@ -85,6 +85,10 @@ function BottomSheetModalComponent<T = never>(
   const currentIndexRef = useRef(!animateOnMount ? index : -1);
   const nextIndexRef = useRef<number | null>(null);
   const restoreIndexRef = useRef(-1);
+  // Local patch (defense-in-depth alongside PR #2720's mount-time fix): mirrors the latest
+  // `handleBottomSheetOnClose` so `handleDismiss`'s watchdog (below) can call it without a
+  // declaration-order problem — `handleBottomSheetOnClose` is defined later in this component.
+  const handleBottomSheetOnCloseRef = useRef<() => void>(() => {});
   //#endregion
 
   //#region variables
@@ -291,6 +295,28 @@ function BottomSheetModalComponent<T = never>(
       statusRef.current = MODAL_STATUS.DISMISSING;
       willUnmountSheet(key);
       bottomSheetRef.current?.forceClose(animationConfigs);
+
+      /**
+       * On Reanimated v4, the animated reaction that drives `<BottomSheet onClose>` (and thus
+       * `handleBottomSheetOnClose` — the only place `statusRef` ever leaves `DISMISSING`) can
+       * silently fail to fire, exactly like the mount-time race PR #2720 fixes. When that
+       * happens here instead, `statusRef` is stuck at `DISMISSING` forever, which makes
+       * `handlePortalRender` (gated on that status) silently no-op for every sheet presented
+       * on this shared modal instance from then on — not just this one. Poll from JS and force
+       * the same close-transition logic if `onClose` never arrives within ~2s.
+       */
+      let attempts = 0;
+      const enforceClosed = setInterval(() => {
+        if (statusRef.current !== MODAL_STATUS.DISMISSING) {
+          clearInterval(enforceClosed);
+          return;
+        }
+        attempts += 1;
+        if (attempts >= 20) {
+          clearInterval(enforceClosed);
+          handleBottomSheetOnCloseRef.current();
+        }
+      }, 100);
     },
     [willUnmountSheet, unmount, key]
   );
@@ -511,6 +537,7 @@ function BottomSheetModalComponent<T = never>(
     },
     [enableDismissOnClose, unmount]
   );
+  handleBottomSheetOnCloseRef.current = handleBottomSheetOnClose;
   //#endregion
 
   //#region expose methods
